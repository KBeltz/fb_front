<article>
  <ul class="fields-list no-list-style">
  {{#each sortedFieldsList as |field|}}
    {{#field-drag-drop content=field.id field=field}}

    {{/field-drag-drop}}
  {{/each}}
  </ul>
</article>