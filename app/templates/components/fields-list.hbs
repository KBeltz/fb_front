<article>
  {{#if showMe}}
    <em>({{yield}})</em>
  {{/if}}
  <button {{action "toggleMe"}}>Toggle</button><br/>
  <strong>Field List</strong>

  <ul>
  {{#each fields as |field|}}
      <li>{{field.fieldName}}</li>
  {{/each}}
  </ul>
</article>