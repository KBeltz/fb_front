import { userAttributeGenerator } from 'poc/helpers/user-attribute-generator';
import { module, test } from 'qunit';

module('Unit | Helper | user attribute generator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userAttributeGenerator([42]);
  assert.ok(result);
});
